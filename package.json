{
  "name": "nuxtProjectTest",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt build",
    "start": "nuxt start",
    "generate": "nuxt generate",
    "deploy": "git add . git commit -m 'dist file' push-dir --dir=dist --branch=main --cleanup"
  },
  "dependencies": {
    "babel-loader": "^8.2.2",
    "core-js": "^3.15.1",
    "nuxt": "^2.15.7"
  },
  "devDependencies": {
    "fiber": "^1.0.4",
    "node-sass": "^6.0.1",
    "push-dir": "^0.4.1",
    "sass": "^1.42.1",
    "sass-loader": "^10.2.0"
  }
}
