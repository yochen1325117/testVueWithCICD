language: node_js
node_js:
 - node
install:
 - npm install
script:
 - npm run build
 - npm run generate
deploy:
  provider: dist
  skip_cleanup: true
  github_token: $GITGUB_TOKEN_TEST
  keep_history: true
  on:
    branch: pageBranch