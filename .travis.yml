language: node_js
node_js:
 - '12'
install:
 - npm install
script:
 - npm run build
 - npm run generate
deploy:
  provider: pages
  skip-cleanup: true
  github-token: $GITHUB_TOKEN_FOR_TRAVIS
  target-branch: pageBranch
  local-dir: dist
  on:
    branch: master